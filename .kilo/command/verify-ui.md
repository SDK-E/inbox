---
description: Verify the UI renders correctly in a real browser using Playwright
agent: browser
subtask: true
---

Verify the current state of the UI by launching a browser via Playwright.

Steps:

1. Ensure `pnpm dev` is running (start it as a background process if not).
2. Check the specified routes render correctly with no console errors, with Playwright.
3. Report findings.

If any issues are found, report them with details (route, error message, suggested fix). Do NOT edit files unless the issue is a trivial fix the user requests inline.
