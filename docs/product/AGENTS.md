# Product Docs Rules

These files define user-visible product intent. Do not change product behavior casually to fit an implementation shortcut.

When implementing product work, read `overview.md`, `principles.md`, `flows.md`, and the relevant PRD. If a requirement is ambiguous, preserve the simplest behavior consistent with these docs and current repository evidence.

Update product docs only when intended behavior changes, not for internal refactors.
